from django.apps import AppConfig


class FormControlConfig(AppConfig):
    """
    The app config for the form_control app.
    """

    name = "form_control"
    verbose_name = "Form control"
    default_auto_field = "django.db.models.BigAutoField"
